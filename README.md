# ext-samples

A number of sample extensions showing how they work

Provided extensions

* `cheddar` - an extension that provides a binary plugin for `jx` using a file at a URL
* `brie` - an extension that provides a binary plugin `jx` using the release of the extension

